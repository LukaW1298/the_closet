package com.project4app.repository;

import com.project4app.model.WashingMode;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WashingModeRepository extends JpaRepository<WashingMode, Long>{
    
}
