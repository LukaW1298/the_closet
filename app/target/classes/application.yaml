spring:
  datasource:
    url: jdbc:mysql://localhost:3306/the_closet?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    show-sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: always